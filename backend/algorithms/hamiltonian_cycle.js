const worker = require("node:worker_threads");

/**
 * @param {Array} graph_matrix A 2D array representing the graph consisting of 0s and 1s or 0s and positive integers if weighted graph
 * @return {Array} An array of all possible permutations of the given array
 */
function hamiltonian_cycle(graph_matrix = [[]]) {}

module.exports = hamiltonian_cycle;
